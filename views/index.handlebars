<h1>Burgers!</h1>

<h2>Burgers I devoured!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burger}}
  {{/if}}
  {{/each}}
</ul>

<h2>Burgers to be devoured!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burger}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Add A BURGER</h2>
<form class="create-form">

  <div class="form-group">
    <label for="new_burger">Burger Name:</label>
    <input type="text" id="new_burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Do you want to devour?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="not_devoured" value="0"> Save for Later.
  </div>

  <button type="submit">Add Burger</button>
</form>